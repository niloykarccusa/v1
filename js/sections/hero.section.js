export function createHeroSection(el) {
    let tl;
    return {
        el,
        onEnter(direction) {
            // tl = gsap.timeline();
            // return tl;
        },

        onLeave(direction) {
            // tl = gsap.timeline();
            // return tl;
        }
    };
}